If you have any questions or need support, you can follow my WeChat Official Account by searching for the ID:clingywang
# Trading System Installation Guide

## 📋 Pre-Installation Requirements

Before using this installer, you need to install the following software yourself:

### 1. MetaTrader 5 (MT5)
- **Download**: https://www.metatrader5.com/
- **Requirement**: Demo or live trading account
- **Note**: This is the trading terminal where your trading strategies will be executed

### 2. Ollama (Local AI Runtime)
- **Download**: https://ollama.com/
- **Version**: Latest recommended
- **Required Models**: Download models based on config.json settings
  - Common models: llama3, mistral, etc.
- **Note**: Ollama runs AI models locally without internet

### 3. Python Environment (if not installed)
- **Download**: https://www.python.org/downloads/
- **Version**: Python 3.10 or higher
- **Must check**: "Add Python to PATH"

### 4. Required Python Packages
Install the following:
```bash
pip install openai metaTrader5 python-dotenv flask
```

---

## 🖥️ System Requirements

| Component | Minimum | Recommended |
|-----------|---------|-------------|
| OS | Windows 10/11 | Windows 11 |
| RAM | 8GB | 16GB+ |
| Storage | 5GB free | 10GB+ |
| Network | Stable connection | Stable connection |

---

## 📦 Installation Steps

### Step 1: Run the Installer
1. Double-click `TradingSystem_Setup.exe`
2. Choose installation directory (recommended: default path)
3. Click "Install" and wait for completion

### Step 2: Install Prerequisites (if not already installed)
Complete installation of the following software:
- [ ] MetaTrader 5
- [ ] Ollama + AI Models
- [ ] Python 3.10+
- [ ] Python dependencies

### Step 3: Configure the System
1. Open the installation directory
2. Edit `config.json` with your settings:
   ```json
   {
     "mt5_login": "your_mt5_login",
     "mt5_password": "your_mt5_password",
     "mt5_server": "your_broker_server",
     "ollama_url": "http://localhost:11434",
     "model": "your_model_name"
   }
   ```

### Step 4: Start the System
Two ways to start:

**Manual Start (Recommended)**
```bash
Double-click start_manual.bat
```

**Auto Start**
```bash
Double-click start_trading_fixed.bat
```

---

## ⚠️ Important Notes

1. **This installer does NOT include:**
   - MetaTrader 5
   - Ollama
   - Python Environment
   - AI Models
   
   You must install these yourself!

2. **First Run**: Test first to ensure all connections work

3. **Security Tips**:
   - Use demo account for first-time testing
   - Password in config.json is stored in plain text - keep it confidential
   - Backup config files regularly

---

## 🔧 Directory Structure

Installed directory structure:
```
TradingSystem/
├── autogpt_trading.py      # Main program
├── executor_agent.py        # Execution agent
├── web_interface.py        # Web interface
├── config.json             # Configuration file
├── mt5_positions.json     # MT5 button positions
├── window_manager.py       # Window manager
├── start_manual.bat       # Manual start script
├── start_trading_fixed.bat # Fixed start script
├── templates/             # Button templates
└── README.md            # Documentation
```

---

## ❓ Troubleshooting

### MT5 Connection Failed
- Check if MT5 is logged in
- Verify mt5_server in config.json

### Ollama Connection Failed
- Ensure Ollama is running
- Check ollama_url in config.json
- Try访问 http://localhost:11434

### Missing Python Packages
```bash
pip install openai metaTrader5 python-dotenv flask
```

---

## 📞 Support

For issues, check:
1. All prerequisite software installed correctly
2. Network connection stable
3. config.json configured correctly

---

*Generated by Automated Trading System Generator*
*Version: 1.0.0*
*Date: 2026-02-21*
